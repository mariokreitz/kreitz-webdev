// Duplicated verbatim in src/index.html's inline anti-FOUC script (no build-time interpolation there) — keep both in sync.
export const PAGE_PREFERENCES_STORAGE_KEY = 'kwd-frontend-page-preferences';
